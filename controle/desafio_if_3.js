const x = 8
const y = 4

const operacao = '*' // + - * /
let resultado

if (operacao === '+')
    resultado = x + y

if (operacao === '-')
    resultado = x - y

if (operacao === '*')
    resultado = x * y

if (operacao === '/')
    resultado = x / y

console.log(resultado)